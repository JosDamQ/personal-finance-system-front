name: finance_app
description: "Personal Finance Management App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  # UI & Icons
  cupertino_icons: ^1.0.8
  material_design_icons_flutter: ^7.0.7296

  # State Management
  provider: ^6.1.1
  flutter_riverpod: ^2.4.9

  # Navigation
  go_router: ^12.1.3

  # Local Database
  sqflite: ^2.3.0
  path: ^1.8.3

  # HTTP & API
  http: ^1.1.2
  dio: ^5.4.0

  # JSON & Serialization
  json_annotation: ^4.8.1

  # Secure Storage
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.2

  # Connectivity & Sync
  connectivity_plus: ^5.0.2

  # Date & Time
  intl: ^0.19.0

  # File & Export (simplified for now)
  path_provider: ^2.1.2
  # pdf: ^3.10.7
  # excel: ^4.0.2

  # Charts & Graphs (simplified)
  # fl_chart: ^0.66.0

  # Image & UI Enhancements (simplified)
  # cached_network_image: ^3.3.1
  # image_picker: ^1.0.7

  # Utils
  uuid: ^4.3.3
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting & Code Quality
  flutter_lints: ^6.0.0

  # Code Generation
  build_runner: ^2.4.7
  json_serializable: ^6.7.1

  # Icon Generation
  flutter_launcher_icons: ^0.13.1

  # Testing
  mockito: ^5.4.4
  sqflite_common_ffi: ^2.3.0
  integration_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter:
  uses-material-design: true

  # Assets
  assets:
    - assets/images/
    - assets/icons/

# App Icon Configuration (commented out for now)
# flutter_launcher_icons:
#   android: "launcher_icon"
#   ios: true
#   image_path: "assets/icons/app_icon.png"
